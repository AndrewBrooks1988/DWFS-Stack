<UserControl x:Class="DWFSWPFUserInterface.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DWFSWPFUserInterface.Views"
             mc:Ignorable="d" Background="White" FontSize="20"
             d:DesignHeight="425" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--Heading-->
        <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="0 0 0 20"
                   HorizontalAlignment="Center" FontSize="48" FontFamily="Segoe UI Light">
            Login Form
        </TextBlock>
        
        <!--Error Message Row-->
        <TextBlock x:Name="ErrorMessage" TextAlignment="Center"
                   Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2"
                   Foreground="Red" MaxWidth="300" TextWrapping="Wrap"
                   Margin=" 0 0 0 20"
                   Visibility="{Binding IsErrorVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}" 
                   FontStyle="Italic" FontSize="18"
                   />

        <!--Username Row-->
        <TextBlock Grid.Row="2" Grid.Column="1"  Margin="0 0 0 10" Padding="0 0 5 0" HorizontalAlignment="Right">
            Username:
        </TextBlock>
        <TextBox x:Name="UserName" Grid.Row="2" Grid.Column="2" MinWidth="175" Margin="0 0 0 10" FontSize="18" />
        
        <!--Password Row-->
        <TextBlock Grid.Row="3" Grid.Column="1"  Margin="0 0 0 10" Padding="0 0 5 0" HorizontalAlignment="Right">
            Password:
        </TextBlock>
        <PasswordBox x:Name="Password" Grid.Row="3" Grid.Column="2" MinWidth="175" Margin="0 0 0 10" FontSize="14" />
        
        <!--Login Button Row-->
        <Button Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Padding="25 5" 
                HorizontalAlignment="Center" x:Name="LogIn" FontFamily="Segoe UI Light" IsDefault="True">
            Login
        </Button>
    </Grid>
</UserControl>
